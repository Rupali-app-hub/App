import React from 'react';
import { View } from 'react-native';

const HorizontalBar = () => {
  return (
    <View
      style={{
        borderBottomColor: '#b072db', 
        borderBottomWidth: 2,       
        marginVertical: 0,         
      }}
    />
  );
};

export default HorizontalBar;
